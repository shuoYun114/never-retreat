# 更新日志

本项目的版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 未发布

### 新增

- **爆炸物对真人玩家生效**：手雷、迫击炮、火箭筒、航弹与坦克炮弹此前只能伤到本地 BOT，对真人完全无效（工兵与反坦克兵在联机里几乎没有输出）。现在由服务端按爆炸威力表裁定：威力、半径与衰减取服务端数值，落点距离、冷却与兵种资格都要校验，火箭筒仅反坦克兵可用。客户端可以额外上报"目标被掩体挡住"，该标记只会降低伤害，因此可以安全采信。
- **记分板显示真人对手与延迟**：按 Tab 现在能看到同房间的真人玩家（★ 自己、● 其他真人、BOT 标注），并显示各自的往返延迟；真人的击杀、阵亡与得分跟随服务端判定实时更新。
- **联机聊天**：`Enter` 全局频道、`U` 队伍频道，大厅与局内均可用。服务端做长度截断、控制字符清理与发送限流，客户端一律以纯文本渲染。
- **真人对战累计战绩**：账号页显示场次、胜负、胜率、累计击杀/阵亡与 K/D。击杀与阵亡由服务端裁定，场次与胜负在对局结束时统一登记（对局需满 2 分钟才计入），单人 BOT 战绩不计入，因此这份数据无法伪造。

### 修复

- 血量不再出现浮点噪声（例如 9.999999999999986），统一保留一位小数。

## [0.2.0] - 2026-07-26

这一版把联机从"能连上"推进到"能正常打"：修掉两个让 PvP 无法进行的阻塞性缺陷，并把伤害、战功、房间准入全部收归服务端裁定。

### 修复

- **阵亡后永久变尸体**：服务端从来没有恢复过血量，玩家第一次死亡后在别人眼里永远是尸体，自己也再也打不出伤害。现在阵亡冷却结束、客户端重新进场即恢复满血。
- **切换战役的成员会被踢出对局**：房主选的战役与成员本地不一致时客户端会重载页面，但服务端拒绝任何人加入已开始的房间，导致该成员再也回不来。现在改为开局前就对齐战役，并允许原成员在宽限期内凭账号重连、拿回原阵营与比分。
- **房主重载会销毁未开局的房间**，已经发出去的 6 位房号随之作废；现在未开局房间同样有断线宽限，房主身份按账号找回。
- **存档解析失败会被空存档覆盖**，等于清空所有账号。现在先备份为 `accounts.json.broken-<时间戳>` 再以空档启动，并在日志中告警。
- **4× 瞄准镜永远装不上任何枪**：所有狙击兵武器都自带瞄具，这件 220 战功的商品实际无法使用。规则改为「可装在没有自带瞄具的栓动步枪上」。
- 消音器的 -8% 伤害此前只在客户端生效，服务端按满伤结算，两边血量对不上。
- 修正启动脚本：`启动联机服务器.cmd` 原先启动的是仓库根目录的旧版单体服务器（端口 8080、没有账号与大厅系统）。
- 自动登录成功后商店与背包不再停留在「请先登录」。
- 房间已关闭时不再把失效的房号继续显示在界面上。

### 安全与反作弊

- **伤害改由服务端裁定**：客户端只上报「使用哪把武器、从哪里往哪个方向射击」，伤害值、爆头倍率、射速一律取服务端武器表。爆头由服务端按几何判定，不再采信客户端标记。此前客户端可自报任意伤害并直接秒杀。
- **武器合法性校验**：按兵种自带清单与账号已购清单校验，无法使用其他兵种的武器。
- **射速限制**：按每把武器的 rpm 计算单发最小间隔。
- **命中判据收紧**：判定半径由约 1.48 m 收紧到 0.92 m，并新增射程上限、单位方向向量校验，以及"开枪点必须贴近本人上报位置"的检查（阻止隔图开枪）。
- **战功无法再刷**：结算加入最短对局时长、打完整局门槛与每日上限；此前循环调用开始/结束接口每次都能白拿 100 战功。击杀发功另加同一受害者重复击杀计数上限，防止双账号对刷。
- **房间准入**：移除客户端从未使用、却可让任意账号凭 6 位房号直接落入他人对局的遗留入口。
- **会话安全**：token 改用 `crypto.randomBytes` 生成（此前为 `Math.random`，可预测），且仅以 sha256 落盘；新增 30 天有效期与过期清理。会话随存档持久化，服务端重启后自动登录依然有效。
- **登录防护**：scrypt 改为异步，不再阻塞事件循环；注册与登录按 IP 与账号分别限速。
- **连接防护**：WebSocket 单连接消息频率超限即断开。

### 稳定性

- 新增进程级 `uncaughtException` / `unhandledRejection` 兜底，各消息处理与接口分别包裹异常捕获；此前任意一处抛错都会导致整个服务进程退出、全服掉线。
- WebSocket 协议处理补齐：拒绝超大帧（此前会把长度字段解析错位）、正确响应 ping/pong 与关闭帧、校验掩码位、支持大于 64 KB 的发送分片，并新增 25 秒心跳与空闲连接回收。
- 连接断开时清理账号-连接映射，修复长期运行下的连接对象堆积。
- 请求体超限时不再泄漏悬挂的 Promise。
- 击杀发放战功由「每次同步重写整个存档」改为 2 秒合并写入。
- 对局记录会清理已结束与陈旧条目，单账号只保留一个进行中的对局。

### 新增

- **大厅新增「规模」选项**：由房主统一决定每方 BOT 数量与兵力票数，此前各客户端各用自己的本地设置。
- **票数与胜负由房主权威广播**：成员不再各自本地结算，同一局的兵力票数与结束时机保持一致；真人阵亡的兵力损失由房主统一扣除。
- **房主修改房间配置后会重置其他成员的准备状态**，避免有人在旧地图上就已准备。
- 新增「退出房间」按钮。
- 新增客户端静态服务 `client/serve.js` 与 `启动客户端.cmd`（默认 18081）。
- 服务端地址自动推导：客户端在 18081 时自动连接同主机的 18080，局域网与公网部署都无需修改配置。
- 新增 `/api/health` 健康检查与 `/api/logout` 接口。
- 新增 `DATA_FILE`、`ALLOW_ORIGIN` 环境变量。
- 桌面客户端支持通过命令行参数、`NEVER_RETREAT_URL` 环境变量或同目录 `server.txt` 指定服务器地址。
- 武器数值表由 `server/tools/gen_weapon_meta.js` 从客户端数据生成，改装兼容规则以同一份文本同时下发给服务端与客户端，两端判定不会再出现分歧（1280 种组合交叉校验一致）。
- 新增 28 个自动化测试：单元测试覆盖账号资产、会话与结算规则；集成测试会真实启动服务端进程，覆盖伤害裁定、射速、复活、房间越权、断线重连、大厅配置同步与洪水防护。运行方式 `cd server && npm test`。

### 已知限制

- 服务端没有世界几何数据，因此**无法校验墙体遮挡**；已通过开枪点位置、射程与命中半径压缩作弊空间，但改造过的客户端理论上仍可穿墙命中。
- 火箭筒、迫击炮与手雷对真人玩家不同步（同步链路目前只覆盖子弹射线）。
- BOT 与载具仍由各客户端本地模拟，位置不同步；联机中同步的是玩家、票数与胜负。

## [0.1.0] - 2026-07-23

首个公开版本。

- 七个战役地图：诺曼底、斯大林格勒、柏林、淞沪会战、百团大战、滇缅公路、莫斯科郊外。
- 六个阵营，各自的武器、坦克、卡车与飞机；八个兵种与 40 把武器。
- 三种玩法：征服、攻防、破袭。
- 载具系统：坦克乘员分工与装甲穿深解算、半履带车／卡车、战斗机与对地攻击。
- 机枪堡、反坦克炮、高射炮、迫击炮等工事，以及可放置的部署物。
- 账号系统、战功商店与兵种配装。
- 房主制联机房间与真人对战。
- 移动端触控布局与陀螺仪辅助瞄准。
